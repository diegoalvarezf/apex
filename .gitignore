.DS_Store
build/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
DerivedData/
*.ipa
*.xcarchive
.build/

# Credenciales locales (nunca subir al repo). Viven en .secrets/ y NO en
# Sources/Apex/Resources: todo lo que hay en Resources se empaqueta dentro de la
# app, así que ahí los secretos viajarían en el .ipa aunque ningún código los lea.
.secrets/
StravaSecrets.plist
